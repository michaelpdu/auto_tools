// --- begin of mid_script ---

this.numPages = Math.max(this.numAddedPages, this.numParsedPages);

try {
    keywords;
} catch (e) {
    this.keywords = "";
}

// --- end of mid_script ---

